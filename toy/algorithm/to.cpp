

T to(string& str, int base = 10)
{
	int point = str.find('.');
	if(point = -1)	
	{

	}

}

// call
// string.to<int>() or str_to<int>(str, 10);

/*
auto s = to<string>(1024);
auto n = to<int>("1024");
auto f = to<float>("3.14");

auto text = to<string>(12, "12", 12.0);

*/